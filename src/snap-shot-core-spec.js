'use strict'

/* global describe, it */
describe('snap-shot-core', () => {
  const snapShotCore = require('.')
  it('write this test', () => {
    console.assert(snapShotCore, 'should export something')
  })
})
